<div class="container">
  <%= simple_form_for (list) do |f| %>
    <%= f.input :name %>
    <%= f.submit %>
  <% end %>

  <hr>
  
  <p><%= link_to "Back to lists", lists_path, class: "btn btn-info" %></p>
</div>