<div class="container">
  <%= simple_form_for (list) do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit %>
  <% end %>

  <hr>
  
  <p><%= link_to "Back to lists", root_path, class: "btn btn-info" %></p>
</div>